import { StyleSheet } from "react-native"

export const CredentialInputStyles
 = StyleSheet.create({
    fieldView : {
        padding: 'auto',
    },
    textInput: {
        height: 40,
        backgroundColor: 'white',
    },
})